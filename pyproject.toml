[project]
name = "parallel-truth-fingerprint-prototype"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["asyncua>=1.1,<2", "minio>=7.2,<8", "numpy>=2,<3"]

[project.optional-dependencies]
runtime-demo = ["paho-mqtt>=2.1,<3"]
ml-training = ["keras>=3,<4", "torch>=2,<3"]
