[project]
name = "parallel-truth-fingerprint-prototype"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["asyncua>=1.1,<2"]

[project.optional-dependencies]
runtime-demo = ["paho-mqtt>=2.1,<3"]
